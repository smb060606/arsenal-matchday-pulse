{
  "name": "arsenal-matchday-pulse",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "predev": "svelte-kit sync",
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "postinstall": "echo skip svelte-kit sync",
    "prebuild": "svelte-kit sync"
  },
  "dependencies": {
    "@atproto/api": "^0.13.22",
    "wink-sentiment": "^5.0.0"
  },
  "devDependencies": {
    "@sveltejs/adapter-vercel": "^5.3.0",
    "@sveltejs/kit": "^2.46.5",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "tslib": "^2.8.1",
    "typescript": "^5.6.3",
    "vite": "^6.3.0"
  }
}
