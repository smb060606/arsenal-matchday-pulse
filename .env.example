# Arsenal Matchday Pulse - Example Environment Variables

# Budget Planner
BUDGET_PER_PLATFORM_DOLLARS="50"
BSKY_COST_PER_MONTH_DOLLARS="0"
# Optional: set to enable planning for these platforms (leave empty to keep unconfigured)
TWITTER_COST_PER_MONTH_DOLLARS=""
THREADS_COST_PER_MONTH_DOLLARS=""

# Cost-to-Cap Estimator (admin-only /api/accounts/plan)
MATCHES_PER_MONTH="8"
PRE_MINUTES="120"
LIVE_MINUTES="105"
POST_MINUTES="60"
BUDGET_SAFETY_BUFFER="1.2"

# Pricing assumptions (only needed if platform is configured)
TWITTER_COST_PER_1K_REQUESTS=""
TWITTER_REQUESTS_PER_ACCOUNT_PER_MIN="1"
THREADS_COST_PER_1K_REQUESTS=""
THREADS_REQUESTS_PER_ACCOUNT_PER_MIN="1"

# Supabase (Client)
PUBLIC_SUPABASE_URL="https://YOUR-PROJECT-ref.supabase.co"
PUBLIC_SUPABASE_ANON_KEY="YOUR_PUBLIC_ANON_KEY"

# Supabase (Server/Admin - NEVER expose to browser)
SUPABASE_URL="https://YOUR-PROJECT-ref.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="YOUR_SERVICE_ROLE_KEY" # keep secret, server-only

# Admin moderation secret (used by /api/comments/moderate)
ADMIN_SECRET="set-a-strong-random-string"

# OpenAI (optional, for summaries API phase)
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-5"
SUMMARIES_TIMEOUT_MS="15000"
SUMMARIES_RATE_WINDOW_MS="60000"
SUMMARIES_RATE_MAX="4"

# Resend (optional, for email notifications)
RESEND_API_KEY=""
RESEND_FROM="notifications@matchday-pulse.dev"

# Bluesky verification (optional)
BSKY_VERIFY_TTL_MS="600000"

# Remote Slack MCP Server
# Slack bot token for posting messages via Slack Web API
SLACK_BOT_TOKEN=""
# Bearer auth token required by /mcp/tools and /mcp/call (set a strong random string)
MCP_AUTH_TOKEN=""

# Summaries API alerting via remote Slack MCP (optional)
# If set, the summaries API will send alerts on rate limit, timeouts, missing key, and failures
SUMMARIES_ALERTS_MCP_BASE_URL=""
SUMMARIES_ALERTS_MCP_TOKEN=""
SUMMARIES_ALERTS_CHANNEL="#cline"
